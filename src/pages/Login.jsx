import React from 'react';
import Nav from '../componetes/Nav';

const Login = () => {
    return (
        <div className="app__flex">
            <Nav />
        </div>
    );
};

export default Login;
